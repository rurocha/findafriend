export interface AuthApi {
  id: number
  name: string
  email: string
  zip_code: string
  address: string
  whatsapp: string
  token: string
}

export interface Auth {
  id: number
  name: string
  email: string
  zipCode: string
  address: string
  whatsapp: string
  token: string
}